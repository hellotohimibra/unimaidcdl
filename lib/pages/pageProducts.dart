import 'package:flutter/material.dart';

class PageProducts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Page Products', textScleFactor: 2.0),
    );
  }
}