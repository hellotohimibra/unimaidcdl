import 'package:flutter/material.dart';

class FlutterPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Page Flutter', textScleFactor: 2.0),
    );
  }
}