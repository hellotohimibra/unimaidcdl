import 'package:flutter/material.dart';

class PageClients extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Page Clients', textScleFactor: 2.0),
    );
  }
}